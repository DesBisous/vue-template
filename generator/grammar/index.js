function integrateGrammar(api, options, files) {
  const grammar = options.grammar;
  switch (grammar) {
    case 'javaScript':
      break;
    case 'typeScript':
      break;
    default:
      break;
  }
}

module.exports = {
  integrateGrammar
}